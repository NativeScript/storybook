<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
  <Page.actionBar>
    <ActionBar title="Plugin Demos" icon="" class="action-bar"/>
  </Page.actionBar>
  <StackLayout class="p-20">
    <ScrollView class="h-full">
      <StackLayout>
        <Button text="storybook-device" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="storybook-web" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="storybook-generator" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
<Button text="storybook" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
      </StackLayout>
    </ScrollView>
  </StackLayout>
</Page>